import 'package:flutter/material.dart';

class Edittodoloadings extends StatelessWidget {
  const Edittodoloadings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
