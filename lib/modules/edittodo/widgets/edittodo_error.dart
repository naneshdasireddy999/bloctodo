import 'package:flutter/material.dart';

class Edittodoerrors extends StatelessWidget {
  const Edittodoerrors({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('something went wrong'),
    );
  }
}
