import 'package:flutter/material.dart';

class Addtodoerrors extends StatelessWidget {
  const Addtodoerrors({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('something went wrong'),
    );
  }
}
